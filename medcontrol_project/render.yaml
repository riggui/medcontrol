services:
  - type: web
    name: medcontrol
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile # << chemin explicite
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: medcontrol_project.settings
      - key: PORT
        value: "10000"
